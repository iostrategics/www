jekyll_chainsmith_www_dev:
    image: jekyll/jekyll
    command: jekyll serve --watch --incremental --config _config.yml,_config.dev.yml
    ports:
        - 4000:4000
    volumes:
        - ../.:/srv/jekyll